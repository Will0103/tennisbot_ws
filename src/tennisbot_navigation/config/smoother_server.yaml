smoother_server:
  ros__parameters:
    use_sim_time: true
    robot_base_frame: base_footprint #modify
    
    simple_smoother:
      plugin: "nav2_smoother::SimpleSmoother"
      tolerance: 1.0e-10
      max_its: 1000
      do_refinement: true